{
  "name": "smart-recipe-finder",
  "version": "1.0.0",
  "private": true,
  "description": "Smart Recipe Finder — Spoonacular-powered recipe search with a Node proxy so the API key stays server-side.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
